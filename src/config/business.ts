// Business configuration - Easy to edit without touching component code

export const businessConfig = {
  // WhatsApp number (format: country code + number, no spaces or signs)
  whatsappNumber: '526442141281',
  
  // Business name
  name: 'Tacos Master',
  
  // Delivery fee in MXN
  deliveryFee: 25,
} as const;
